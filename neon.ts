import { defineConfig } from "@neon/config/v1";

/**
 * PascoAI Neon Infrastructure Configuration
 * Project: PascoAi (ID: dawn-dew-88231934)
 * Organization: Pasco Ai (ID: org-holy-tooth-35915732)
 * 
 * Strict Zero-Trust Lakebase PostgreSQL Configuration.
 * Note: Neon Auth is explicitly disabled per security architecture (uses PascoAI custom zero-trust auth & RLS).
 */
export default defineConfig({
  // Lakebase Postgres configuration
});
